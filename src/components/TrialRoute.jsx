// La navegación y lectura son públicas. Las pruebas gratuitas se consumen
// únicamente en el momento de ejecutar una acción de valor (buscar, comparar o decidir).
export default function TrialRoute({ children }) {
  return children;
}
